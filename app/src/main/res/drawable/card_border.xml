<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Warna background dalam card -->
    <solid android:color="@android:color/white"/>

    <!-- Border -->
    <stroke
        android:width="2dp"
        android:color="@color/md_theme_primary"/>  <!-- ganti dengan warna yg kamu mau -->

    <!-- Biar sama kayak corner CardView -->
    <corners android:radius="16dp"/>
</shape>
